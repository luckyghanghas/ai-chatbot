# Fake News Detector

A simple NLP project that classifies a news-style text as likely real or fake using a Naive Bayes style approach.

## Features

- Tokenizes and cleans input text
- Trains from a built-in dataset
- Predicts fake or real label
- Returns a simple confidence score

## Run

```bash
python app.py
```
